const controller = require("../controller");
const express = require("express");
const feedRouter = express.Router();

// // 피드 업로드 /feeds/upload
// feedRouter.post("/upload", controller.upload);

// // 피드 수정 /:id
// feedRouter.patch("/:id", controller);
// // 피드 조회
// feedRouter.get("/:id", controller);
// // 피드 삭제
// feedRouter.delete("/:id", controller);

// // 알 카운트 조회 /:id/egg
// feedRouter.get("/:id/egg", controller);
// // 알 카운트 수정
// feedRouter.patch("/:id/egg", controller);
// // 댓글 조회  /:id/comment
// feedRouter.get("/:id/comment", controller);
// // 댓글 등록
// feedRouter.post("/:id/comment", controller);
// // 댓글 수정
// feedRouter.patch("/:id/comment", controller);
// // 댓글 삭제
// feedRouter.delete("/:id/comment", controller);

// export default feedRouter;